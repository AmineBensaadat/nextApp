module.exports = {
  i18n: {
    defaultLocale: 'en', // Default language (no prefix)
    locales: ['en', 'fr'], // Other languages
    localeDetection: false, // Disable automatic language detection
  },
  reloadOnPrerender: true, // Ensure changes take effect during development
};
